import React from "react";
import { Text, View } from "react-native";

const Wave = () => {
  export enum Side {
    LEFT,
    RIGHT,
    NONE,
  }
  return (
    <View>
      <Text>Wave</Text>
    </View>
  );
};

export default Wave;
